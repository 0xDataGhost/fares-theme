/**
 * Global chrome interactions (search toggle, drawer open/close).
 * Populated in Phase 2 — placeholder keeps the manifest entry valid.
 */
( () => {
	'use strict';
	document.documentElement.classList.add( 'fares-js' );
} )();
